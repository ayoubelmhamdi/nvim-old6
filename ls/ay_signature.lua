signature_config = {
  log_path = vim.fn.expand "$HOME" .. "/tmp/sig.log",
  debug = false,
  hint_enable = true,
  -- handler_opts = {border = "single"},
  max_width = 80,
  -- add time to toogel
  auto_close_after = 3,
}
